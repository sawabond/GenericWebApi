name: Publish to Azure

on:
  workflow_dispatch:
  push:
    branches:
    - master
    
env:
  AZURE_WEBAPP_NAME: generic-web-api
  AZURE_WEBAPP_PACKAGE_PATH: "./publish"

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .net
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'
          
      - name: Restore
        working-directory: ./src/api
        run: dotnet restore GenericWebApi.sln
        
      - name: Build
        working-directory: ./src/api
        run: dotnet build GenericWebApi.sln --configuration Release --no-restore
        
      - name: Publish
        working-directory: ./src/api
        run: dotnet publish GenericWebApi.sln --configuration Release --no-build --output '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}'
      
      - name: List all files
        working-directory: ./src/api
        run: ls -R
        
      - name: Deployment
        uses: azure/webapps-deploy@v2
        with:
          working-directory: ./src/api
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          package: "${{ env.AZURE_WEBAPP_PACKAGE_PATH}}"
